interface:
  display_name: "Human in the Loop"
  short_description: "Design auditable human approval gates"
  default_prompt: "Use $human-in-the-loop to design auditable approval gates and escalation paths for this agent workflow."
